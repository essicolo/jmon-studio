// Moved from src/algorithms/io/jmon/conversion.js
// ...existing code...

export class JMonConverter {
	static midiToNoteName(midi) {
		// Dummy implementation, replace with actual logic
		return `MIDI_${midi}`;
	}
	static timeToMusicalTime(time) {
		// Dummy implementation, replace with actual logic
		return `${time}`;
	}
}
